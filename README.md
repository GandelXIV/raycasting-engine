# raycasting-engine
	A simple in terminal ray-casting engine

# usage
	Compile using make.
	Test in bin/.
	Linkable lib in mk/.
	Header file in src/engine.h (depends on src/shared.h and src/structs.h)
	
	
